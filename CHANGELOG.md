# Changelog

## [v8.2] - 2026-03-25

### 🚨 Critical Bugfix Release: v8.0 데이터 로드 불가 해결 + 기능 통합

이 버전은 GitHub Pages(v8.0) 배포에서 데이터가 전혀 로드되지 않던 치명적 버그를 수정하고,
v8.0의 멀티소스/프로필 기능과 v8.1의 Kit Manager 개선사항을 완전히 통합합니다.

### 🔴 치명적 버그 수정 (v8.0 → v8.2)

| 버그 | 원인 | 수정 |
|---|---|---|
| `init()` 함수 미실행 | `settings-modal` HTML 누락으로 app.js 파싱 단계에서 크래시 | index.html에 Settings Modal HTML 추가 |
| `GAS_WEB_APP_URL undefined` | data.js에서 프로필 시스템으로 교체 후 state.js 일부 함수에서 잔존 참조 | 모든 파일에서 `getActiveGASUrl()` 통일 |
| Cloud 데이터 로드 0건 | 위 두 버그의 연쇄로 `fetchCloudData()` 호출 자체가 이뤄지지 않음 | init() 복구로 정상화 |

### 🟢 v8.0 → v8.2 통합: Multi-Source Data Loader & Profile Manager

- **Cloud / Local / Demo 모드 전환**: 헤더 소스 스위처로 데이터 소스를 즉시 전환
- **프로필 매니저**: ⚙️ 설정 버튼 → 여러 Google Sheets 프로필 CRUD (추가/이름변경/삭제/선택)
- **Local Storage 모드**: 오프라인 환경에서 데이터 편집 후 로컬 저장 가능
- **Demo 모드**: 5개 계정 · 12개 스트림 · 37건 타임라인 샘플 데이터 (읽기 전용)
- **Demo 모드 가드**: Demo 모드에서 계정/스트림/킷 수정 시 alert로 안내

### 🟡 v8.1 → v8.2 병합: Kit Manager 안정화

- **Kit Manager 크래시 수정**: `kits` flat array를 2차원 배열로 잘못 접근하던 로직 전면 수정
- **실시간 전역 동기화 (`syncGlobalKitsToStreams`)**: 킷 항목 추가/삭제/이름변경 시 모든 계정의 `stageEfforts` 즉시 반영
- **이름 변경 시 체크 상태 보존**: Rename 시 이전 완료(V) 상태가 새 이름으로 자동 매핑
- **인덱싱 버그 수정**: wildcard(`*`) 항목 삭제/수정 위치 오차를 `indexOf` 기반 정밀 매칭으로 해결
- **Kits Cloud Sync**: 킷 변경 시 Google Sheets Kits 시트에 자동 저장 (`postKitsToCloud`)

### 🔵 신규 기능 (v8.2)

- **솔루션 컬러 레전드**: 맵 하단에 솔루션별 색상 범례 표시, 클릭으로 필터 토글 가능
- **Discovery Gap 시각화**: Early Sales 완료율 50% 미만인 스트림에 ⚠️ 경고 표시 (맵 도트 + 계정 목록)
- **맵 도트 색상**: 계정 인덱스 기반 → **솔루션 고유 색상** 기반으로 개선 (OCP=빨강, RHEL=초록 등)

### 🟢 UI/UX

- 헤더 레이아웃 정리: 타이틀 + 소스 스위처 + 프로필 셀렉터 인라인 배치
- 설정 모달 개선: 설명 텍스트, 입력 placeholder 상세화
- 버전 캐시 버스터: `?v=8.2`

---

## [v8.1] - 2026-03-24 *(로컬 only — GitHub 미배포)*

### 🚀 핵심 업데이트: Kit Manager 근본적 개선

- Kit Manager 크래시 해결 (flat array 잘못 접근 수정)
- 실시간 데이터 동기화 (`syncGlobalKitsToStreams`) 도입
- 이름 변경 시 체크 상태 보존
- Kits Cloud Sync (`postKitsToCloud`) 추가

---

## [v8.0] - 2026-03-22 *(GitHub 배포 — 데이터 로드 버그 존재)*

### 신규 기능 (미동작 상태로 배포됨)

- Multi-Source Data Loader (Cloud/Local/Demo)
- Profile Manager (Google Sheets 다중 연결)
- Discovery Gap Detection
- 솔루션 컬러 맵

### 알려진 버그 (v8.2에서 수정)

- `settings-modal` HTML 누락으로 `init()` 실행 실패
- `GAS_WEB_APP_URL` 참조 오류로 클라우드 연결 불가
- 결과: 빈 화면, 데이터 0건

---

## [v7.4] - 2026-03-20

- Kit Manager 구현 (세일즈킷 항목 추가/삭제/수정)
- NBA(Next Best Action) 추천 버튼
- 동적 세일즈킷 파이프라인 (Google Sheets Cloud metadata)
- 제품별 세일즈킷 필터링 (`getKitForStream()`)

---

*Generated by Guru (AI Assistant) — 2026-03-25*
